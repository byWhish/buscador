export const baseEndpoint = 'http://localhost:8000/api/v1/';

export const locale = {
    country: 'AR',
    language: 'es',
    currency: 'ARS',
}

export const urls = {
    items: '/items/',
}

export const routes = {
    gridItems: '/items/',
    dipItem: '/items/:id',
}

export const PAGE_STATE = {
    LOADING: 'loading',
    DONE:   'done',
    ERROR:  'error',
}